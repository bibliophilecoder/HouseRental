{
  "name": "househunt",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone HouseHunt app",
  "scripts": {
    "start": "node app.js",
    "dev": "npm run dev --prefix ./Client",
    "client": "npm run dev --prefix ./Client",
    "build": "npm run build --prefix ./Client",
    "server": "npm start --prefix ./Server"
  }
}
